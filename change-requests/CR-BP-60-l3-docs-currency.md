# CR-BP-60: Documentation Currency for the Landed L3 Layer

**Status**: Proposed
**Layer**: Process Catalog (documentation)
**Owner**: Coder (for eaojnr)
**Date**: 2026-09-14
**Carrier**: Documentation slice; brings the narrative docs into agreement with the landed L3 (Activity) layer and the canonical L1 Process Group records.
**Depends on**: CR-BP-12 (PG model), CR-BP-19 (register v2), CR-BP-32 (Activity Model), CR-BP-42..57 (L3 landings)
**Lands against**: 126 canonical BP records, 35 canonical PG records, 35 canonical PC records, 501 canonical Activity records; 23 conformance gates CONFORMANT; v0.3.0 tagged (CR-BP-41)

---

## 1. Change Request

Refresh the decomposition narrative in `README.md` §3 and `docs/architecture.md` to reflect the landed catalog state. Pre-fix staleness:

- Both diagrams listed Activity as "(CR-BP-04; future)"; Activity has been canonical since CR-BP-42 (501 records under CR-BP-32's ACT-001..010 gate).
- Both diagrams described L1 Process Group as "conceptual; documented here" only; PGs have been canonical records since the CR-BP-19 register landings (35 records, PG-001..008 gate, made non-vacuous by CR-BP-58).
- The architecture CR mapping pointed at placeholders CR-BP-04 (Activity Model) and CR-BP-05 (Execution Boundary); the landed models are CR-BP-32 and CR-BP-33.
- Paths referenced `contexts/v1/` / `entities/v1/`; the live layout is `contexts/v1-alpha/` / `entities/v1-alpha/`.

Post-fix state:

- Hierarchy presented as L0 (conceptual) / L1 / L2 / L3 (canonical records) with L4 (Workflow/Task) correctly marked future and metamodel-owned (`dea:Task` lifecycle: proposed).
- L2-to-L3 linkage documented per ACT-010 (`metadata.activity_references[]` / `belongs_to_business_process`).
- L3 coverage statement: 126 BPs, 501 Activities, all seven ECF domains, `decomposition_boundary: l4-reached` per CR-BP-32 §12.

## 2. Mechanics

1. `README.md` §3: retitle, redraw the hierarchy diagram, add the L3 coverage paragraph, correct directory wording.
2. `docs/architecture.md`: redraw the decomposition diagram, extend the L1 section with the canonical-record note, add an L3 Activity section, update the structural-vs-execution CR mapping.
3. No gate, schema, CI, or record change. Record counts unchanged (ACT 501 / EXE 662 / report 697).
4. Documentation conformance (DOC-001..004, advisory gate): per-line finding comparison against pre-change main shows **0 genuinely new findings** in the edited files (remaining findings are pre-existing legacy-terminology advisories, unchanged in kind).

## 3. Gate posture

- Full suite: **23 gates, 0 blocking, 0 advisory failures** (documentation gate remains advisory).
- pytest: **368 passed**.
- `check_cr_metadata.py`: 19 legacy, **0 new**.

## 4. Repository changes

| Path | Status | Notes |
|---|---|---|
| `README.md` | MOD | Section 3 decomposition narrative refreshed |
| `docs/architecture.md` | MOD | Decomposition diagram + L1/L3 sections + CR mapping |
| `reconciliation/baseline/v1.yaml` | MOD | Regenerated (no entity hash changes; routine regen) |
| `reconciliation/conformance_report.yaml` | MOD | Regenerated by gate [10]; 697 records (unchanged) |
| `CATALOG.yaml` | MOD | Regenerator bumps `open_change_requests` 72 -> 73 |
| `change-requests/CR-BP-60-l3-docs-currency.md` | NEW | Slice carrier CR (this document) |
| `change-requests/README.md` | MOD | CR-BP-60 row added |

## 5. What this CR is NOT

- **NOT a remediation of the 600+ legacy DOC-001 terminology advisories.** Those predate this slice and are a separate program decision.
- **NOT a record, gate, schema, or CI change.** Only narrative documentation moves.
- **NOT an L4 (Task) landing.** `dea:Task` remains metamodel-owned (lifecycle: proposed); the docs now say so explicitly.
- **NOT a count-assertion change.**

## 6. Acceptance criteria

1. README §3 and docs/architecture.md describe Activity as landed (CR-BP-32; ACT-001..010) and Process Group as canonical records.
2. No reference to Activity as "future" remains in either file.
3. Full pytest suite passes; `scripts/conformance_result.py` remains CONFORMANT (23 gates).
4. `python3 scripts/check_cr_metadata.py` reports 0 new findings.
5. Documentation conformance per-line findings in edited files: 0 genuinely new.

## 7. Result

CR-BP-60 closes the documentation debt opened by the L3 program: the two primary narrative documents now match the canonical tree (126 BPs, 501 Activities, 35 PGs, 35 PCs) and correctly mark L4 as future and metamodel-owned.
