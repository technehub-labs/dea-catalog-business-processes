{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://technehub-labs.github.io/dea-catalog-processes/schemas/entities/process-group.schema.json",
  "title": "ProcessGroup",
  "description": "Schema for Process Group (L1) entries in technehub-labs/dea-catalog-processes. CR-BP-12; downstream of CR-BP-11 (L1 Process Group discovery register) and CR-BP-04 (ID-family contract). A Process Group is a catalog-owned record type that organizes Business Process specializations within a Process Context (Domain x Lifecycle Stage cell). The Process Group is NOT a metamodel entity; it is catalog-owned and lives only in this catalog. The canonical containment direction is `L1 group --composes--> L2 process`; the inverse `part_of` is a derived view recorded on the L2 side for navigation. ID family: `dea:group-*` (CR-BP-04 §4).",
  "type": "object",
  "required": [
    "id",
    "type",
    "name",
    "definition",
    "process_context",
    "scope",
    "outcomes",
    "composes",
    "process_group_kind",
    "status",
    "lifecycle_status",
    "version"
  ],
  "properties": {
    "type": {
      "type": "string",
      "const": "ProcessGroup",
      "description": "Discriminator. Always 'ProcessGroup' for Process Group entries (mirror of `dea:BusinessProcess` / `type: Process` on L2)."
    },
    "id": {
      "type": "string",
      "pattern": "^dea:group-[a-z0-9-]+$",
      "description": "Stable Process Group id. CR-BP-04 §4 family. Convention: dea:group-<kebab-name>. The id must be stable across renames of name/definition; renames go through the CR-programme."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "definition": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2000,
      "description": "Normative semantic meaning of this Process Group. Not a description; a definition. PG-006 (MECE) depends on this being non-overlapping with peer Process Groups in the same Process Context."
    },
    "process_context": {
      "type": "string",
      "pattern": "^dea:pc-[a-z0-9-]+$",
      "description": "Reference to the canonical Process Context (dea:pc-*) within which this Process Group organizes L2 processes. PG-003 enforces resolution to a known context."
    },
    "scope": {
      "type": "object",
      "required": ["includes", "excludes"],
      "properties": {
        "includes": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Explicitly included enterprise responsibilities."
        },
        "excludes": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Explicitly excluded responsibilities (belong in adjacent contexts or peer Process Groups)."
        }
      },
      "description": "Inclusion / Exclusion boundaries (PG-006 / MECE)."
    },
    "outcomes": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Intended enterprise outcomes for this Process Group. The outcomes distinguish this Process Group from peer Process Groups in the same Process Context."
    },
    "composes": {
      "type": "array",
      "description": "Canonical containment relationships: every entry declares that this Process Group composes a Business Process specialization. Each entry conforms to the metamodel relationship-instance.json (source_id, target_id, relationship_type, plus optional direction, status, effective_from/to, asserted_by, rationale, evidence, provenance). PG-004 (target_id pattern) and PG-005 (target resolution) enforce.",
      "items": {
        "type": "object",
        "required": ["source_id", "target_id", "relationship_type"],
        "properties": {
          "source_id": {
            "type": "string",
            "pattern": "^dea:group-[a-z0-9-]+$"
          },
          "target_id": {
            "type": "string",
            "pattern": "^dea:process-[a-z0-9-]+$",
            "description": "Target Business Process specialization id (dea:process-*)."
          },
          "relationship_type": {
            "type": "string",
            "const": "composes",
            "description": "The Process Group's canonical containment relationship is `composes`. Other relationship types are not permitted in this array."
          },
          "direction": {
            "type": "string",
            "enum": ["source-to-target"],
            "default": "source-to-target"
          },
          "status": {
            "type": "string",
            "enum": ["proposed", "planned", "active", "deprecated", "retired"],
            "default": "active"
          },
          "effective_from": {"type": "string", "format": "date"},
          "effective_to": {"type": "string", "format": "date"},
          "asserted_by": {"type": "string"},
          "rationale": {"type": "string", "maxLength": 1000},
          "evidence": {"type": "string", "maxLength": 1000},
          "provenance": {
            "type": "object",
            "properties": {
              "type": {"type": "string", "enum": ["architecture-review", "discovery", "assessment", "inference", "migration", "manual"]},
              "reference": {"type": "string"},
              "asserted_by": {"type": "string"},
              "asserted_at": {"type": "string", "format": "date"}
            }
          }
        }
      }
    },
    "process_group_kind": {
      "type": "string",
      "enum": [
        "end-to-end",
        "functional",
        "support",
        "cross-cutting",
        "governance",
        "innovation"
      ],
      "description": "Process Group classification. Controlled vocabulary at classifications/process-group-kinds.yaml. PG-007 enforces."
    },
    "status": {
      "type": "string",
      "enum": ["candidate", "accepted", "deferred", "deprecated", "rejected"],
      "description": "Discovery / governance status (mirrors CR-BP-11 register disposition values)."
    },
    "lifecycle_status": {
      "type": "string",
      "enum": ["candidate", "active", "deprecated", "retired"],
      "description": "Catalog entry lifecycle. `active` requires at least one composes entry with status: active (PG-008)."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "evidence": {
      "type": "array",
      "description": "Optional: structured evidence supporting the Process Group definition.",
      "items": {
        "type": "object",
        "required": ["source", "claim", "strength"],
        "properties": {
          "source": {"type": "string"},
          "claim": {"type": "string"},
          "strength": {"type": "string", "enum": ["E0", "E1", "E2", "E3", "E4", "E5"]},
          "url": {"type": "string", "format": "uri"}
        }
      }
    },
    "grouping_basis": {
      "type": "object",
      "description": "Optional: explicit grouping basis declared by CR-BP-95. When present, PG-009 enforces structural integrity. Back-compat with pre-CR-BP-95 PG records: the field is OPTIONAL and existing records remain conformant.",
      "required": ["type", "statement"],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "value-stream",
            "functional-responsibility",
            "support-capability",
            "cross-cutting-concern",
            "governance-obligation",
            "innovation-candidate"
          ],
          "description": "Controlled vocabulary for the grouping basis type. The six values mirror the existing process_group_kind vocabulary (classifications/process-group-kinds.yaml) but are stated as a declarative grouping principle rather than a classification."
        },
        "statement": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1000,
          "description": "A declarative sentence describing the process-centric grouping principle. Example: 'Groups processes by the customer-facing value stream they participate in.'"
        }
      }
    },
    "membership_criteria": {
      "type": "object",
      "description": "Optional: explicit membership criteria declared by CR-BP-95. When present, PG-010 enforces structural integrity. Back-compat: OPTIONAL.",
      "required": ["inclusion_test", "exclusion_test"],
      "properties": {
        "inclusion_test": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1000,
          "description": "A testable predicate that determines whether a candidate L2 Business Process belongs in this Process Group. Example: 'A Business Process belongs in this group if its trigger is a customer-initiated request.'"
        },
        "exclusion_test": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1000,
          "description": "A testable predicate that determines whether a candidate L2 Business Process is explicitly excluded from this Process Group. Example: 'A Business Process is excluded from this group if its trigger is regulator-initiated.'"
        }
      }
    },
    "metadata": {
      "type": "object",
      "properties": {
        "cross_context_overlap": {
          "type": "array",
          "items": {"type": "string", "pattern": "^dea:pc-[a-z0-9-]+$"},
          "description": "Optional: explicit declaration of other Process Contexts this Process Group participates in via cross-context membership. PG-006 enforces intent vs. accident."
        },
        "established_by": {"type": "string"},
        "established_at": {"type": "string", "format": "date"},
        "change_history": {
          "type": "array",
          "items": {"type": "object"}
        }
      }
    },
    "links": {
      "type": "array",
      "description": "Optional: navigation links to related artifacts (documentation, change requests, research register). Same shape as in L2 entity entries.",
      "items": {
        "type": "object",
        "required": ["rel", "href"],
        "properties": {
          "rel": {"type": "string"},
          "href": {"type": "string"}
        }
      }
    },
    "ecfConformance": {
      "type": "object",
      "description": "Optional: ECF conformance posture. Process Groups inherit the catalog posture by default; only declare this block if the Process Group has ECF conformance specifics distinct from the catalog. CR-ECF-CG-004 §10.",
      "properties": {
        "framework": {"type": "string"},
        "contractVersion": {"type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$"},
        "profile": {"type": "string"},
        "status": {"type": "string"},
        "affiliation": {"type": "string"},
        "canonicalReferences": {
          "type": "array",
          "items": {"type": "object"}
        }
      }
    }
  },
  "additionalProperties": false
}